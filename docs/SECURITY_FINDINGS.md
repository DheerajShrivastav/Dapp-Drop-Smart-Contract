# Security & Correctness Findings — Web3Campaigns

> Original code-audit findings, annotated with current status through the `cancelCampaign` feature pass (`feature/cancel-campaign`, forked from `dev` post-invariant-hardening merge). Verify line numbers before acting.

## Status legend
✅ FIXED · 🟡 OPEN · 🔵 INTENTIONAL (founder decision) · ⏳ ADDRESSED IN PROGRESS

## HIGH

1. 🔵 **`grantHostRole` is unguarded** (CampaignManagement.sol) — anyone can self-grant `HOST_ROLE`. **Left open intentionally by founder decision (for now).** Revisit when adding the staked/curated hosting model.
2. ✅ **ERC20 rewards pulled from host's live wallet** — **FIXED in Stage B1.** Rewards are now escrowed in the contract (`fundCampaignERC20`) and paid from escrow via Merkle settlement (`claimERC20`). No host-pull, no allowance-revoke brick, no rug. See [[REWARD_SYSTEM]].
3. ✅ **Cross-campaign ERC20 escrow drain via `claimERC20` after sweep** — **FOUND AND FIXED by the escrow-solvency invariant suite** (`test/invariant/EscrowSolvency.invariant.t.sol`), not by manual review, and was **live on `dev`** (shipped in B1) before this fix. `claimERC20` never checked `_erc20Swept` despite a code comment claiming it did. Because ERC20 escrow is a single commingled token balance (unlike NFTs, which are earmarked per campaign), after a host swept campaign A's unclaimed funds back to themselves post-grace, a late claimant on A could still claim — paid out of **campaign B's** escrow, leaving B underwater. Fixed: `claimERC20` now reverts `AlreadySwept` once a campaign has been swept. Regression test: `test_ClaimERC20_BlockedAfterSweep_NoCrossCampaignDrain`.

## MEDIUM

4. ✅ **Silent zero-reward claims** (FCFS/TIERED/pool-exhaustion) — **FIXED** for ERC20 (B1) and NFT (B2): only winners with a valid proof can claim; `InsufficientEscrow`/`NFTNotEscrowed` revert rather than silently paying 0. Legacy NFT pool path removed in B2.
5. ✅ **On-chain hold verification broken** (52-byte vs 64-byte `abi.decode`) — **FIXED in Stage A.** `completeTask` now requires the correct 64-byte `abi.encode(address,uint256)` for `ONCHAIN_HOLD_ERC20/721`. (Tests in `test/StageAFixes.t.sol`.)
6. ✅ **Claim-rank front-running** (TIERED/FCFS earliest-claimer advantage) — **FIXED** for ERC20 (B1) and NFT (B2): allocations are predetermined in the Merkle tree, not by claim order.
7. ✅ **`ONCHAIN_TX` task hard-reverts / bricks campaigns** — **FIXED in Stage A.** No longer reverts in `completeTask` (now `NotSelfVerifiable`); is settled via `verifyTaskCompletionWithSignature`/`batchVerifyTaskCompletionWithSignatures` (Phase 2 — replaced the old host-tx `verifyTaskCompletion`/`batchVerifyTaskCompletion`). `ONCHAIN_HOLD_*` remain self-verified and signer-unoverridable.
14. ⏳ **Merkle root allocation fairness has no on-chain recourse** — **PARTIALLY ADDRESSED** via `ROOT_DISPUTE_WINDOW` (24h): `claimERC20`/`claimNFT` reject claims against a root until that window elapses since its (re-)publish, giving the community time to catch an unfair allocation and escalate (e.g. `emergencyPause`) before funds move. This is a **delay-based mitigation, not full on-chain dispute resolution** — there is still no on-chain flagging/veto mechanism, just time for off-chain detection. See the "New surface" note below.

## LOW / INFORMATIONAL

8. ✅ **`_suspiciousActivityScore` dead state** — **FIXED in Stage A.** Wired via `MODERATOR_ROLE` + `Web3Campaigns.flagAccount(user, score)` + `AccountFlagged` event; gate in `completeTask` now functional.
9. ✅ **`endCampaign` comment/code contradiction** — **FIXED in Stage A** (comment corrected; behavior = end only at/after `endTime`, no early end).
10. ✅ **`withdrawETH` arbitrary `_to`** — **FIXED.** `withdrawETH` no longer takes a caller-supplied destination; it sweeps to a stored, admin-settable `_treasury` (`setTreasury`, rejects `address(0)`; `withdrawETH()` reverts `TreasuryNotSet` while unset). Clears Slither's `arbitrary-send-eth` finding. See the hardening-sweep pass note below.
11. ✅ **Unguarded external-call loops** — claims are single-transfer (ERC20 `safeTransfer`; NFT one token per claim). Deposit/sweep loops are host-only, capped at 100/call, and `nonReentrant`.
12. ✅ **Pause coverage gap on `createCampaign`** — **FIXED in Stage A** (virtual + `whenNotPaused` wrapper). The other reward/host functions were already pause-gated via the virtual `onlyHost` override.
13. 🟡 `== 0` existence checks (id-sentinel `incorrect-equality`) — low risk by design; unchanged.

## Not yet implemented (deferred)
N-of-M threshold signing, sybil gating (humanity-module design agreed, not yet built). A full on-chain dispute-resolution mechanism for Merkle roots (a time-delay mitigation exists — see #14 above). Protocol fee, `.code.length` token checks, gasless claims (sponsored `claimERC20For`/`claimNFTFor`/`claimRewardFor` — see the sponsored-claims pass below), and `MAX_PARTICIPANTS_LIMIT` enforcement (see the participant-limit pass below; `JOIN_COOLDOWN` was removed rather than implemented, also below) are now implemented — removed from this deferred list.

## New surface added in the cancelCampaign pass (review focus)
- **`cancelCampaign` — bait-and-switch griefing path closed by zero-participants gate.** The function is only callable while `totalParticipants == 0`. Once one participant has completed any task the campaign is permanently locked in (`CampaignHasParticipants`). This was an explicit founder requirement: without the gate a host could let participants do free work then cancel right before `Ended` to avoid paying out. Tested with an explicit abuse-vector test (`test_CancelCampaign_RevertsOnceAParticipantHasEngaged` in `test/CancelCampaign.t.sol`).
- **ERC20 refund via `_refundERC20IfAny` — immediate, no grace period.** Safe because `cancelCampaign` is only reachable while `totalParticipants == 0`, which means no Merkle root could ever have been published (roots are set during Ended, which requires prior Open state where participants could have engaged) and no claim was ever possible. The `_erc20Swept` flag is set atomically with the transfer to prevent double-refund.
- **NFT reclaim — no auto-enumeration.** Escrowed NFTs are not auto-returned (no on-chain per-campaign inventory list). The host must call `withdrawUnclaimedERC721`/`withdrawUnclaimedERC1155` with the specific tokenIds they know they deposited. `_requireSweepable` now returns early for `Cancelled` status (no grace wait needed, same rationale as ERC20 above).
- **`Cancelled` is a new terminal status.** No state transition out of `Cancelled` exists. Guards on Open/Ended/Closed-requiring functions all implicitly exclude it; `_requireSweepable`'s early-return is the only deliberate `Cancelled`-aware branch. Verify any future state guards also account for this fifth status.

## New surface added in the invariant-test pass (review focus)
- **Escrow-solvency invariant found finding #3 above** (cross-campaign ERC20 drain via `claimERC20` after sweep). This is the first bug this project's invariant/fuzz testing has caught — see `test/invariant/EscrowSolvency.invariant.t.sol`.
- **NFT inventory invariant confirmed no equivalent bug** for ERC721/ERC1155: `claimNFT` and `withdrawUnclaimedERC721/1155` check the *same* per-campaign escrow map, so a swept slice is already zeroed before a late claim could reach it. Fuzzed with a shared ERC1155 asset id across many campaigns specifically to stress the commingled-pool case (`test/invariant/NFTInventory.invariant.t.sol`).
- **Attestation-version invariant confirmed the signed-attestation replay guard holds** under adversarial fuzzing (stale-version replay, skip-ahead, non-signer, expired-deadline attempts) — the on-chain version only ever advances by exactly 1 via a genuinely valid next-version signature (`test/invariant/AttestationVersion.invariant.t.sol`).
- Foundry `[invariant]` config added (`foundry.toml`): 128 runs, depth 30, `fail_on_revert = false` (handler calls are expected to revert on bounded edge cases; ghosts stay consistent on discard).

## New surface added in Phase 2 (review focus)
- **`SIGNER_ROLE` key compromise** — a compromised signer can mint arbitrary task completions until revoked via `revokeRole(SIGNER_ROLE, ...)`. There is no on-chain rate limit or threshold; single-signer by design. Rotate quickly if compromised. See [TASK_VERIFICATION.md](TASK_VERIFICATION.md).
- **Zero-address participant guard** — `verifyTaskCompletionWithSignature` explicitly rejects `_participant == address(0)` (`Web3Campaigns__ZeroAddress`).
- **Signature replay/update model** — per-`(participant,campaign,task)` version counter; replay is impossible because the accepted version is advanced atomically. A signer *can* flip `completed` back to `false` by signing the next version — intentional (correction/re-verification path), but means partial completion counts may decrease for any off-chain aggregation that queries task state.
- **`completeTask` guard** — if any attestation has ever been accepted for a `(participant, campaign, task)` triple (`version > 0`), `completeTask` reverts `TaskManagedBySignature` to prevent mixing self-assertion and signer-controlled state.
- **Domain binding** — `verifyingContract` is encoded in the EIP-712 domain, so a signature produced for one deployment cannot validate against another. Tested in `test/SignatureVerification.t.sol`.

## New surface added in B1–B2 (review focus)
- ✅ **Escrow accounting now measures the amount actually received, not the nominal funding amount — FIXED.** `fundCampaignERC20` used to credit `_erc20Escrowed` with the caller-supplied `amount`, assuming a standard (non-fee-on-transfer) ERC20; a fee-on-transfer token would silently under-fund escrow. Fixed via `balanceOf(this)` before/after the pull — see the "fee-on-transfer ERC20 support" pass below.
- **Merkle root is host-controlled and updatable while Ended** — a malicious host can publish a root that omits/short-changes users (off-chain trust in the host's allocation). Escrow only guarantees funds *can't be rugged after commitment*, not that the allocation is fair. **Mitigated by `ROOT_DISPUTE_WINDOW` — see #14 and the pass note below.** Applies to both ERC20 and NFT roots.
- **NFT cross-campaign isolation** is enforced by the per-campaign `_escrowedERC721`/`_escrowedERC1155` ownership maps, now on `NFTSettlementModule` (tested: `test_ClaimNFT_CannotDrainNonEscrowedToken`). A campaign can only ever settle NFTs actually escrowed under it.
- NFT custody relies on OZ `ERC721Holder`/`ERC1155Holder`; `supportsInterface` resolves the `AccessControl` + `ERC1155Holder` diamond.
- ERC1155 over-allocation in a root is bounded by escrow: a leaf exceeding the escrowed balance reverts `NFTNotEscrowed` rather than over-paying.

## New surface added in the on-chain reward module + per-campaign pinning pass (review focus)
- **Single global `_onChainRewardModule` pointer was a mid-campaign rotation hazard (FIXED).** On-chain tiered settlement keeps all rank/score/tier state in a separately-deployed `OnChainRewardModule`, referenced by the admin-rotatable `_onChainRewardModule`. When that single pointer was the authorization basis for payouts, rotating it mid-campaign could **desync in-flight rank/score state** (a freshly-registered module has no record of completions the old one already tallied) or **lock an in-flight campaign out of payouts entirely** (the state-holding old module is no longer the registered one). Fix: each campaign **pins** its module once at adoption (`_campaignRewardModule[id]`, set in `setSettlementMode`, decoupled from the global default), and `payOnChainReward` authorizes against that pin alone (`RewardModuleMismatch`) — a rotation never moves authority for already-adopted campaigns. Tests: `test_PayOnChainReward_PinnedModuleStillPaysAfterRotation` / `_RevertsFromRotatedGlobalModule` / `_UnpinnedCampaignRejectsAllCallers` (`test/OnChainRewardModule.t.sol`).
- **`claimReward` self-check is defense-in-depth for a currently-UNREACHABLE path — do not remove it blind.** The module re-verifies `getCampaignRewardModule(id) == address(this)` before computing any payout (`OnChainRewardModule__NotAuthoritativeModule`). Today a campaign's pin **cannot be reassigned after adoption**, so no live path exists where an authoritative module holds stale state for a campaign that has moved elsewhere — this guard exists specifically so a **future** change adding re-pinning / module migration cannot silently reopen the desync/lockout class above without that author confronting it here. Test: `test_ClaimReward_NonAuthoritativeModuleReverts`.

## New surface added in the root dispute window pass (review focus)
- **`ROOT_DISPUTE_WINDOW` (24h) gates claims, not sweeps.** `claimERC20`/`claimNFT` reject claims against a root until `ROOT_DISPUTE_WINDOW` has elapsed since it was (re-)published (`Web3Campaigns__RootDisputeWindowActive`). Sweeps (`withdrawUnclaimedERC20`/`withdrawUnclaimedERC721`/`withdrawUnclaimedERC1155`) were deliberately **not** given a separate explicit check: a sweep requires `Closed` + `CLAIM_GRACE_PERIOD` (30 days) after closing, and roots can only be set/updated while `Ended` (frozen at `Closed`) — so as long as `ROOT_DISPUTE_WINDOW` stays well below `CLAIM_GRACE_PERIOD` (documented at the constant), the dispute window on the final root has always long since elapsed by the time a sweep is reachable. If a future change ever shrinks `CLAIM_GRACE_PERIOD` close to or below `ROOT_DISPUTE_WINDOW`, this ordering assumption should be re-verified.
- **Rearmed only when the root VALUE actually changes — FIXED after review.** A host publishing a genuinely different root (e.g. fixing a bad allocation) restarts the window from scratch rather than inheriting the original publish's now-elapsed clock — an updated allocation deserves its own review period. The initial version rearmed on *every* call to `setERC20MerkleRoot`/`setNFTMerkleRoot` regardless of whether the root value changed; a manual PR review (not `no-mistakes`) caught that this let a host repeatedly "update" to the byte-identical root to indefinitely push out `claimableAt`, dangling participants at a *visibly published* root that never actually opens — a stronger complaint than the pre-existing "never publish at all" host-trust boundary, since it looks like progress without being any. Fixed: `setERC20MerkleRoot`/`setNFTMerkleRoot` now compare against the currently-stored root and only rearm on a genuine change. Tests: `test_ClaimERC20_SameRootRepublishDoesNotRearmWindow` / `test_ClaimNFT_SameRootRepublishDoesNotRearmWindow`.
- **Still not a full dispute-resolution system.** There is no on-chain flagging/veto actor for a specific root; the only escalation lever during the window is the existing blunt `EMERGENCY_ADMIN.emergencyPause()` (halts everything, not root-specific) or the host self-correcting. The window's value is purely giving off-chain verification time to happen before funds move.
- **Two new sentinel `== 0` checks** (`getERC20ClaimableAt`/`getNFTClaimableAt` returning 0 when no root was ever published) follow the same accepted low-risk-by-design pattern as #13 (`campaign.id == 0` etc.) — flagged by Slither's `incorrect-equality` detector, consistent with the rest of the codebase, not a new risk.

## New surface added in the protocol fee module pass (review focus)
- **`FeeModule` is a satellite contract, not inline logic — deliberately, for size reasons.** `Web3Campaigns` was at ~1.25KB headroom (24,576B EIP-170 limit) before this feature; a protocol fee added inline would have consumed most or all of it. Following the `OnChainRewardModule` precedent, fee computation lives in a separately-deployed `FeeModule` with its own budget; `Web3Campaigns` only integrates the narrow `IFeeModule.computeFee` call site, `setFeeModule`/`getFeeModule`, storage, two errors, and two events — costing ~503B of entrypoint headroom (now ~747B remaining, including the post-review `InvalidFeeTreasury` guard). Any future feature needing meaningful logic will very likely need the same satellite treatment.
- **No per-campaign pinning — a deliberate, reasoned omission, not an oversight.** The reward module needed pinning because it holds persistent per-campaign state (rank/score) that a rotation could desync from the pin used at payout time. `computeFee` has no equivalent: it is a pure function of `(amount, the module's current global config)`, fully evaluated and settled (fee transferred, escrow credited) within the single `fundCampaignERC20` call that invoked it. A rotation only affects funding calls made *after* it — the intended behavior, not a hazard. Test: `test_FeeModuleRotation_OnlyAffectsFutureFundingCalls`.
- **`computeFee` is a `view` call — reentrancy is structurally foreclosed, not just guarded.** Solidity compiles a call to an interface method declared `view` as a `STATICCALL`; the EVM itself reverts any `SSTORE` (or other state-changing opcode) attempted during that call, regardless of what the module's implementation tries to do. A malicious `_feeModule` cannot reenter `fundCampaignERC20` (or anything else) from inside `computeFee`.
- **Two defensive guards on `computeFee`'s return values, both unreachable through the reference `FeeModule` but reachable through any *other* module registered via `setFeeModule`** — an admin-trust boundary, same class as `_onChainRewardModule`: a misconfigured or malicious module can only harm campaigns funded after it is registered, and only up to what a host actually transfers.
  - `feeAmount > amount` → `Web3Campaigns__FeeExceedsAmount` (capped at `MAX_FEE_BPS = 1000`, i.e. 10%, enforced in both the reference module's constructor and `setFeeBps`). Converts a would-be arithmetic underflow Panic into a named, diagnosable error. Test: `test_FundCampaignERC20_RevertsIfFeeExceedsAmount` (deliberately-evil mock).
  - `feeAmount > 0 && treasury == address(0)` → `Web3Campaigns__InvalidFeeTreasury` (the reference module's constructor and `setTreasury` both reject `address(0)`). **Added after PR review** — the original version guarded `feeAmount` but not `treasury`, so a misbehaving module returning a nonzero fee with a zero treasury would have hit `safeTransfer(address(0), feeAmount)` directly: a self-inflicted funding DoS on standard OZ tokens (which revert on transfer-to-zero), or a silent burn of the skimmed fee on a permissive/non-standard token. Test: `test_FundCampaignERC20_RevertsIfTreasuryIsZeroAddress` (deliberately-evil mock); `test_FundCampaignERC20_ZeroFeeWithZeroTreasury_Succeeds` confirms the guard only fires when there's an actual fee to send.
- **`setFeeModule`'s missing zero-check on `_module`** (flagged by Slither) is the identical accepted pattern as `setOnChainRewardModule`: `address(0)` is a valid, intentional value that disables the feature entirely, not an oversight.
- **`FeeModule.admin` is a genuinely separate trust boundary from Web3Campaigns' `DEFAULT_ADMIN_ROLE` — deliberate, but deploy-time-relevant.** Documented in `FeeModule.sol`'s header: the module's `admin` is intentionally not required to be Web3Campaigns' own admin, so a project can run fee governance under separate keys/multisig if desired. The consequence worth being deliberate about: if the SAME key ends up holding both roles in practice, compromising that one key now also controls the fee rate and destination, not just campaign administration — a larger blast radius than existed before this feature. **Recommendation for production deployments: use a distinct key (ideally a multisig) for `FeeModule.admin`, separate from whoever holds `DEFAULT_ADMIN_ROLE` on `Web3Campaigns`.** No code change — this is a key-management decision made at deploy time, not something the contract can or should enforce (enforcing it would remove the deliberate flexibility to run fee governance separately).
- **`CampaignFundedERC20` now emits the NET escrowed amount, not the gross amount transferred from the host** (they're identical when no fee module is registered). No existing test asserted this event's payload before this change, so nothing broke — but any *new* off-chain indexer consuming this event should sum it together with `ProtocolFeeCollected` to reconstruct the host's gross payment.
- **Aderyn's High-1 bucket (reentrancy: state change after external call) gained a 7th instance, not a new category.** `computeFee` (a `view` call → `STATICCALL`) is flagged there alongside the four pre-existing `OnChainRewardModule` entries and two `ParticipantManagement` entries — a false positive, same class as those. Aderyn's category totals (High 1, Low 15) are unchanged, but "unchanged" should not be read as "no new finding anywhere" — precisely this one bucket did grow by one instance.

## New surface added in the NFT settlement module extraction pass (review focus)
- **`NFTSettlementModule` is a satellite contract, not inline logic — same size-reclaiming motivation as `FeeModule`, but larger blast radius since it's a custody-adjacent module, not a pure computation.** `Web3Campaigns` was at ~747B headroom before this pass; all NFT Merkle-settlement logic and per-campaign escrow bookkeeping was extracted into a separately-deployed `NFTSettlementModule` (its own EIP-170 budget, 4,664B used). `Web3Campaigns` reclaimed ~793B (now ~1,540B headroom) and kept **100% of NFT custody** — the module never holds tokens, only decides who gets paid what and calls back into the trusted `executeNFTTransferOut`. Unlike `FeeModule`, users call the module's entrypoints (`setNFTMerkleRoot`/`claimNFT`/`withdrawUnclaimedERC721`/`withdrawUnclaimedERC1155`) directly, not through a `Web3Campaigns` forwarding wrapper — mirroring `OnChainRewardModule.claimReward`'s shape, not `FeeModule.computeFee`'s.
- **Per-campaign pinning is required here (unlike `FeeModule`) — and pins at FIRST DEPOSIT, earlier than the reward module's pin-at-mode-adoption.** The module holds persistent per-campaign escrow state (which tokens/amounts are held for which campaign) that accumulates starting at the very first `depositERC721Rewards`/`depositERC1155Rewards` call — before any Merkle root could ever exist. Pinning at root-set time (mirroring the reward module) would leave a window between an unpinned deposit and a later root-set where a global-module rotation could orphan already-recorded escrow. `CampaignManagement._pinNFTModule` closes that window by pinning on the first deposit instead, idempotently, via `_campaignNFTModule[id]`. `Web3Campaigns.executeNFTTransferOut` authorizes solely against this per-campaign pin (`Web3Campaigns__NFTModuleMismatch` otherwise), identical in spirit to `_campaignRewardModule`/`RewardModuleMismatch`.
- **A direct, deliberate consequence: a campaign that never deposits an NFT can never call `setNFTMerkleRoot`.** It reverts `NFTSettlementModule__NotAuthoritativeModule` (the campaign was never pinned to any module). This is a strictly **stronger** security property than the pre-extraction behavior, not a regression — it blocks a cross-campaign-drain attempt one step earlier (at root-publish time, instead of only at claim time via the pre-existing per-campaign escrow-ownership check). Tests: `test_SetNFTMerkleRoot_RevertsOnNeverDepositedCampaign`, and `test_ClaimNFT_CannotDrainNonEscrowedToken` was updated to have the attacker's campaign deposit an unrelated token first (to get pinned) before publishing a fraudulent root, preserving the original drain-guard intent under the new pinning requirement.
- **Defense-in-depth self-check, same currently-unreachable-path caveat as the reward module.** Every `NFTSettlementModule` entrypoint re-verifies `getCampaignNFTModule(id) == address(this)` before acting on its own state (`NotAuthoritativeModule`). As with `OnChainRewardModule.claimReward`'s equivalent check, no live path exists today where an authoritative module holds stale state for a campaign pinned elsewhere (pins are set-once, never reassigned) — the guard exists so a **future** re-pinning/migration feature cannot silently reopen the desync bug class without its author confronting this check.
- **`IWeb3CampaignsForNFTModule.CLAIM_GRACE_PERIOD()`/`ROOT_DISPUTE_WINDOW()` are cross-contract getter calls, not direct constant references — a Solidity limitation, not a design choice.** `ContractName.publicConstantName` syntax only works for enums/library members without inheritance; a public constant on another (non-inherited) contract requires an actual external call through its auto-generated getter. Both are declared as `external view` on the interface and called via `IWeb3CampaignsForNFTModule(WEB3_CAMPAIGNS).X()`, same pattern already used for `getCampaignHostAndStatus`/`getCampaignClosedAt`/`getCampaignNFTModule`.
- **Aderyn's High-1 bucket (reentrancy: state change after external call) gained instances in `NFTSettlementModule`, not a new category.** The flagged lines are local escrow-state writes (`_nftLeafClaimed`, `_escrowedERC721`/`_escrowedERC1155`) following `view`/`STATICCALL` reads (`getCampaignHostAndStatus`, `getCampaignNFTModule`) earlier in the same function — the same false-positive class already accepted for `OnChainRewardModule`/`FeeModule`. Slither's naming-convention findings also grew (268 total, up from 249) purely from the new module's `_underscore`-prefixed parameters, the same already-accepted convention used everywhere else in this codebase.

## New surface added in the fee-on-transfer ERC20 support pass (review focus)
- **`fundCampaignERC20` now measures what actually landed via `balanceOf(this)` before/after the pull, instead of trusting the caller-supplied `amount` — FIXED, closing a documented-but-open gap.** Standard ERC20s make this a no-op (received == amount, byte-for-byte unchanged behavior and event payloads). A fee-on-transfer token skims its own cut in transit; crediting `_erc20Escrowed` with the nominal `amount` in that case would silently over-credit escrow beyond what the contract can ever actually pay out — a latent insolvency, not just a display bug, since `claimERC20`/`withdrawUnclaimedERC20` both pay from `_erc20Escrowed` accounting that would no longer be backed 1:1 by the contract's real token balance. New error `Web3Campaigns__NoFundsReceived` for the degenerate 100%-fee case (nothing arrives).
- **This necessarily reorders `fundCampaignERC20`'s CEI shape: the transfer-in interaction now happens BEFORE the escrow-accounting effect, not after — a deliberate, documented exception, not a lapse.** The received amount is only knowable once the pull completes, so the effect (`_erc20Escrowed[_campaignId] += escrowAmount`) cannot be computed any earlier. This is the same balance-diff pattern used throughout DeFi for the identical reason (Uniswap V2's `mint`/`burn`, and every other fee-on-transfer-aware vault). Safety comes from `nonReentrant` on the `Web3Campaigns.fundCampaignERC20` wrapper, not from strict instruction ordering — standard ERC20s (including fee-on-transfer ones) have no reentrancy hook to exploit here regardless, but the guard is what makes the exception unconditionally safe rather than merely "safe today."
- **Protocol fee (if a `FeeModule` is registered) is now computed on the RECEIVED amount, not the nominal one — consistent with the escrow fix, not a separate change.** `computeFee(id, received)` instead of `computeFee(id, amount)`; the contract can never be asked to skim a fee on funds it never actually custodied. A side effect worth knowing: the fee-payout leg (`safeTransfer(treasury, feeAmount)`) is itself just another transfer of the same token, so if that token is fee-on-transfer, the treasury nets less than `feeAmount` — the escrow accounting stays internally consistent regardless (the full `feeAmount` is debited from the contract's balance either way), it's only the treasury's *received* amount that can differ from the emitted `ProtocolFeeCollected(feeAmount)` value for such tokens. Not fixed further in this pass (would require repeating the same balance-diff defense on every outbound leg in the codebase); flagged here so a future change touching fee payout doesn't assume the emitted amount always equals what the treasury received.
- **Aderyn's High-1 bucket (reentrancy: state change after external call) gained 2 more instances on `fundCampaignERC20`'s existing entry (9 → 11 total), not a new category.** The `_erc20Escrowed` write is now flagged against three preceding calls instead of one (`balanceOf` ×2, `computeFee`) since the balance-diff pattern is itself two additional STATICCALLs before the same effect — same false-positive class, same `nonReentrant` mitigation, already accepted throughout this codebase. Aderyn's totals are otherwise unchanged (High 1 / Low 15, matching the pre-pass baseline). Slither gained exactly one new finding, `received == 0` (`incorrect-equality`) — the same accepted low-risk-by-design sentinel-check pattern as every other `== 0` finding in this codebase (#13).
- **New test file `test/FeeOnTransferERC20.t.sol`** with a minimal `FeeOnTransferMock` ERC20 (skims a configurable bps to a burn sink on every transfer) covers: escrow credited with the received amount not the nominal one, a 0%-fee token behaving identically to a standard token (regression guard), multiple funding calls accumulating correctly, the protocol-fee-on-received-amount interaction, and the `NoFundsReceived` degenerate case (153 tests total, up from 148).

## New surface added in the fee-on-transfer invariant coverage pass (review focus)
- **A 6th stateful-fuzz suite, `FeeOnTransferEscrowSolvency.invariant.t.sol`, fuzzes the same three escrow-solvency properties as `EscrowSolvency.invariant.t.sol` against a fee-on-transfer ERC20 instead of a standard token.** Follow-up flagged (non-blocking) during PR #12's review: the 5 fixed-point unit tests in `test/FeeOnTransferERC20.t.sol` cover the received-vs-nominal accounting fix correctly, but the invariant suite specifically built to catch escrow/balance drift across long random fund/claim/sweep sequences (`EscrowSolvencyHandler`) only ever wired up a standard `ERC20Mock`, so it couldn't exercise this exact scenario. This pass closes that gap with a dedicated handler/suite pair rather than parametrizing the existing one over two token types — mirrors this codebase's established pattern of one dedicated suite per concern (`RootDisputeWindow`, `NFTInventory`, `OnChainReward` are all separate files too), and avoids the added complexity of mixed-token per-campaign accounting in a single handler.
- **`FeeOnTransferEscrowSolvencyHandler` diverges from `EscrowSolvencyHandler` in exactly one structural way: ghost accounting tracks the amount ACTUALLY received per funding call, not the nominal amount requested** — measured via the same `balanceOf` before/after pattern the contract itself uses internally. The per-campaign allocation cap (`fundAmount / 4` in the original handler) is sized off the received amount instead, so claims stay non-vacuous (sum of allocations never exceeds what was actually escrowed) rather than bouncing off `InsufficientEscrow` for every fuzzed campaign.
- **Validated non-vacuous with a negative control before merge, following this project's established practice** (same discipline as `OnChainReward.invariant.t.sol`'s pinned-module negative control): temporarily reverted `fundCampaignERC20` itself (in `src/CampaignManagement.sol`, not the handler) to trust the nominal `_amount` instead of the measured `received` amount — reintroducing the exact bug PR #12 fixed, with the handler's own ghost tracking left correct — and confirmed `invariant_perCampaignBacked` failed immediately on the very first funding call in the fuzzed sequence, proving the suite genuinely catches a regression in the production fix rather than passing by construction. Reverted before merge. **Correction from PR #13's review**: an independent reproduction attempt broke the *handler's* `ghost_totalFunded` tracking instead of the contract (a different, also-valid experiment, but not the one actually run for this PR) and got a different failing invariant, `invariant_globalTokenAccounting` — expected, since that invariant is the one that reads `ghost_totalFunded` directly, while `invariant_perCampaignBacked` reads escrowed/distributed straight from the contract and never touches that ghost variable. Both invariants are real and both catch real regressions; they're just wired to different failure modes (contract-side bug vs. test-harness-side bug).
- **Distributed/escrowed ledger amounts are unaffected by the token's own skim on the claim/sweep outbound legs** — `_erc20Distributed`/`_erc20Escrowed` are debited by the full requested amount either way (the token's `_update` hook deducts the full amount from the sender's balance regardless of how much the recipient nets), so `invariant_distributedLeqEscrowed` and the balance-based invariants hold without needing separate accounting for what a participant/host actually received versus what left the contract's balance. Same nuance already documented for the protocol-fee-payout leg in the fee-on-transfer ERC20 support pass above.

## New surface added in the hardening-sweep pass (review focus)
Three small independent items bundled into one pass (each too small for its own PR). Two touch `src/` (contract-size and finding-baseline relevant), one is test-only.
- **`withdrawETH` no longer takes an arbitrary destination — closes finding #10 / Slither's `arbitrary-send-eth`.** Replaced `withdrawETH(address payable _to)` with a no-argument `withdrawETH()` that sweeps to a stored `_treasury`, set via a new admin-only `setTreasury(address)` (rejects `address(0)`; `withdrawETH()` reverts `Web3Campaigns__TreasuryNotSet` while the treasury is unset). New `TreasuryUpdated` event, new `getTreasury()` view. **This is an API-breaking change** to `withdrawETH` (drops its parameter) — acceptable because the function is `DEFAULT_ADMIN_ROLE`-only, emergency-use, and there is no live deployment yet. Slither `arbitrary-send-eth` confirmed gone (269 → 268 total results); Aderyn unchanged (High 1 / Low 15 — `withdrawETH` stays in the accepted L-1 Centralization Risk bucket as an admin function, which is unchanged in kind). Tests: `test_WithdrawETH_Success` (now sets a treasury first), `test_WithdrawETH_RevertsIfTreasuryNotSet`, `test_SetTreasury_RevertsOnZeroAddress`/`_OnlyAdmin`/`_StoresAndRotates`.
- **`.code.length` guard on reward-token addresses at the configuration boundary.** New internal `_requireContract(address)` (reverts `Web3Campaigns__NotAContract` on an address with no deployed code) applied in `configureERC20Reward`, `depositERC721Rewards`, and `depositERC1155Rewards`, in addition to the existing `== address(0)` checks (kept — a zero address trivially has no code, but the dedicated `InvalidTokenAddress` error is the clearer diagnostic for that specific mistake). Catches a fat-fingered/wrong-address configuration at the boundary rather than letting it fail opaquely later inside a transfer. **Explicitly NOT a well-behaved-token guarantee** — it only proves code exists *now*, not that the address is a conforming or non-malicious ERC20/721/1155; that trust boundary is unchanged. Also does not defend against an address whose contract is deployed *after* configuration (e.g. a CREATE2 pre-computed address), which remains a host-trusts-their-own-input case. Tests: `test_ConfigureERC20Reward_RevertsOnEOA`, `test_DepositERC721_RevertsOnEOA`, `test_DepositERC1155_RevertsOnEOA` (plus a zero-address regression test for `configureERC20Reward`).
- **Multi-leaf NFT Merkle proof coverage.** All prior NFT claim tests used single-leaf roots (empty proofs), so `MerkleProof.verify`'s actual multi-node path was only exercised on the ERC20 side. `test_ClaimERC721_MultiLeafTree_BothParticipantsClaim` builds a real balanced 2-leaf tree (two participants, two distinct escrowed tokenIds) and claims each with a genuine single-sibling proof; `test_ClaimERC721_MultiLeafTree_RevertsOnWrongProof` confirms a malformed proof over the same tree is rejected (`InvalidMerkleProof`). Test-only — no `src/` change.
- **Contract size**: `Web3Campaigns` 23,258B → 23,499B (~241B for the treasury setter/getter/event/errors + the `_requireContract` guard), ~1,077B headroom remaining. Test-count: 160 → 166.

## New surface added in the sponsored-claims (gasless) pass (review focus)
- **Three new permissionless `...For` claim entrypoints: `claimERC20For` (Web3Campaigns), `claimNFTFor` (NFTSettlementModule), `claimRewardFor` (OnChainRewardModule).** Each shares its ENTIRE body — checks, effects, payout — with the corresponding self-claim function via an internal refactor (`_claimERC20`/`_claimNFT`/`_claimReward`), with the account parameter substituted for `msg.sender` throughout. All 166 pre-existing tests passed unchanged after the refactor, confirming behavior preservation on the self-claim paths.
- **Why permissionless is safe here — the core security argument to re-verify on any future change:** what's claimable is bound to the *account*, never the caller. The ERC20/NFT Merkle leaves commit to `(account, ...)`; the tiered payout derives from the participant's own rank/score state; and payment/delivery always targets the account (`safeTransfer(_account, ...)`, `executeNFTTransferOut(..., _account)`, `payOnChainReward(_campaignId, _participant, ...)`). A hostile caller can only deliver an account's own allocation to that account's own wallet. **If any future change lets a `...For` caller influence the recipient or the amount, this argument collapses** — that's the invariant to check in review.
- **Accepted residual: forced-claim griefing.** A third party can claim *for* you without your consent — you receive tokens you didn't ask for, at a time you didn't choose (potentially relevant for tax timing or for contracts that account for received tokens). This is the standard trade-off in permissionless airdrop-claim designs and was accepted deliberately; the alternative (an authorization signature from the account) would reintroduce per-claim signing UX, defeating the gasless purpose. Note one asymmetry: for a *contract* recipient, a forced NFT claim can still revert on the receiver hook (safeTransferFrom checks), but a forced ERC20 claim cannot be refused.
- **No meta-transaction framework — deliberate.** ERC-2771 (`ERC2771Context`) would override `_msgSender()` across the entire inheritance tree (AccessControl included), a large blast radius for what sponsored submission achieves with ~30 lines. ERC-4337 needs no contract change at all and remains available to users with smart accounts independently of this feature.
- **Size cost:** `Web3Campaigns` 23,499B → 23,678B (~179B for the inline ERC20 wrapper + zero-check; **~898B headroom remaining — now under 1KB**). `claimNFTFor`/`claimRewardFor` land in their satellites' own budgets (both still ~20KB free). The next inline addition to `Web3Campaigns` needs a size check *before* writing code.
- **Slither: 268 → 285 (+17), all accepted classes** — naming-convention entries for the new functions' `_underscore` params, plus the pre-existing timestamp/reentrancy-events/unused-return entries re-attributed from the old public claim functions to the renamed internal `_claim*` bodies. No new category. Aderyn unchanged (High 1 / Low 15).
- **Tests:** `test/SponsoredClaims.t.sol` (17) — for each of the three paths: pays the account not the caller, sponsored↔self double-claim exclusion (shared claimed-state), non-allocated/unqualified account rejection, zero-address rejection, pause coverage, and (ERC20) dispute-window enforcement on the sponsored path. **Correction from PR review**: the initial version of this PR asserted pause coverage was tested "for each of the three paths," but `test_ClaimRewardFor_RevertsWhenPaused` was actually missing — `claimRewardFor`'s pause behavior was correct (it routes through `payOnChainReward`, whose `whenNotPaused` wrapper on `Web3Campaigns` was untouched by this PR) but untested. A reviewer traced the code, confirmed the behavior held functionally, and flagged the doc/test mismatch; the missing test was added rather than the claim being walked back.

## New surface added in the reward-configuration invariant coverage pass (review focus)
- **New 7th stateful-fuzz suite, `RewardConfigAccessControl.invariant.t.sol`, closes the last open item on the hardening backlog** (`docs/NEXT_STEPS.md`: "access-control fuzzing on `configureERC20Reward`/deposits is still open"). Every prior invariant suite fuzzes a SINGLE host (the handler contract itself), so per-campaign host-**binding** — `campaign.host == msg.sender`, independent of merely holding `HOST_ROLE` — was never exercised end-to-end. This suite runs 3 distinct hosts, each owning its own campaigns, and repeatedly has each attempt `configureERC20Reward`/`depositERC721Rewards`/`depositERC1155Rewards` on the OTHER hosts' campaigns — calls that must always revert `CallerIsNotHost`, since `onlyHost`'s ownership check runs before any status/ownership logic in the function body, regardless of campaign state or whether the attacker even owns anything to deposit.
- **A significant methodological finding, discovered via this suite's own negative control: this project's established "loud revert on unexpected success" pattern (`AttestationVersionHandler`, `RootDisputeWindowHandler`) does not actually fail an invariant run under this project's `fail_on_revert = false` profile.** The reasoning documented for that pattern — that it's more reliable than a bare `vm.expectRevert`, which gets silently discarded on a mismatch — is correct as far as it goes, but incomplete: **any** revert from a handler call, including a deliberate `revert("...")` signaling a caught bug, is *also* silently discarded by the fuzzer under `fail_on_revert = false`, and everything written during that same call — including the ghost-counter increment the pattern relies on to signal the bug — is rolled back with it. Verified directly: the first version of this suite used the established pattern and a hand-broken `onlyHost` (host-check replaced with `if (false)`) produced **zero test failures** — the bypass genuinely happened (confirmed via a separate live-state cross-check) but the suite reported all-green.
- **Fix used in this suite (not retroactively applied to the three pre-existing suites — flagged here, not silently fixed, since that's a separate, larger change with its own regression risk):** don't revert in the unexpected-success branch at all. Record the outcome in a ghost counter and let the call return normally, so the write survives; check the ghost counter via `assertEq` inside a view `invariant_*` function, which fails the run through forge-std's non-reverting failure mechanism (a storage-flag write the runner checks after return, not a revert) rather than through control flow the fuzzer itself intercepts. `invariant_UnauthorizedAttemptsNeverSucceed` also tracks a second failure mode this same fix surfaced: an attacker call that still reverts post-bypass, but for the *wrong* reason (e.g. failing on NFT-ownership grounds instead of `CallerIsNotHost`, once the intended access-control gate is gone) — `ghost_wrongRevertReason`, not just `ghost_unauthorizedBypasses`, both asserted `== 0`.
- **Recommended follow-up (not done here, scope discipline):** re-verify `AttestationVersionHandler`'s and `RootDisputeWindowHandler`'s loud-revert branches the same way (temporarily reintroduce the bug each was built to catch, confirm the *suite* fails, not just that a trace shows a revert) and convert any that don't to the ghost-counter-only pattern above.
- **Independent cross-check invariant, not just the handler's own bookkeeping:** `invariant_ConfiguredTokenMatchesLegitimateHostOnly` reads `getERC20Settlement(id).token` directly from the live contract and compares it against the value the campaign's real host last set — this is what actually caught the negative control's `configureERC20AsAttacker` mutation (the *other* invariant caught it too, via `ghost_wrongRevertReason`, on a coincidentally-reverting sibling call in the same fuzzed sequence — two independent signals from one bug, not one).

## New surface added in the loud-revert re-verification pass (review focus)
- **The follow-up flagged in the previous pass is done: `AttestationVersionHandler.sol` and `RootDisputeWindowHandler.sol` both actually had the fragile-under-`fail_on_revert=false` loud-revert bug, confirmed by negative control, and both are now fixed.** For each, temporarily reintroduced the exact bug the affected branch was built to catch, confirmed the *suite* reported zero failures with the old pattern, converted to the ghost-counter-only pattern (record outcome, return normally, assert via `invariant_*`), and confirmed the same negative control now correctly fails.
- **`AttestationVersionHandler`'s exposure was partially masked by pre-existing cross-checks, but not fully.** `invariant_versionMatchesGhostModel`/`invariant_completionMatchesLastAcceptedAttestation` are independent live-state checks that don't reference the attacker functions' own bookkeeping — but they were *also* blind to a bypass, because the loud `revert("...")` doesn't just erase a ghost-counter write, it erases the **entire call frame**, including the exploited contract's own state mutation (the version/completion change a successful bypass produced). Disabling the `hasRole(SIGNER_ROLE, signer)` check or the deadline check each independently produced **zero failures** across all three invariants with the old pattern. Fixed: `attestNonSigner`/`attestExpiredDeadline`/`attestReplayStaleVersion`/`attestSkipAheadVersion` no longer revert on unexpected success; a new `ghost_unexpectedAcceptances` counter (asserted via a new `invariant_noUnexpectedAcceptances`) is explicit and redundant with the pre-existing cross-checks, which — now that the mutation survives to be observed — also correctly catch the same bypass on their own.
- **`attestReplayStaleVersion`/`attestSkipAheadVersion` were never actually exploitable, independent of the revert issue — kept for a different reason.** `verifyTaskCompletionWithSignature` takes no caller-supplied version at all; the contract always computes `nextVersion = current + 1` itself and embeds that in the digest it recomputes, so a signature built for any other version can never recover to a valid signer. Converted to the same non-reverting shape anyway, as a regression check against a hypothetical future refactor that accepted an explicit version parameter.
- **`RootDisputeWindowHandler` had NO independent cross-check at all** — its two pre-existing invariants (`invariant_ERC20ClaimableAtMatchesGhostRearmRule`/`invariant_NFTClaimableAtMatchesGhostRearmRule`) only assert the *timing* rule (`getERC20ClaimableAt`/`getNFTClaimableAt` match the ghost rearm model), never that an actual claim respected it. This made it a strictly worse case than `AttestationVersionHandler`: with the old pattern, disabling either dispute-window check (`ParticipantManagement.sol`'s ERC20 path or `NFTSettlementModule.sol`'s NFT path) produced **zero failures**, full stop — nothing in the suite was watching. Fixed: `attemptERC20Claim`/`attemptNFTClaim`'s three branches each (should-succeed-but-reverted, should-revert-but-succeeded, reverted-for-the-wrong-reason) now record into six ghost counters instead of reverting, asserted by a new `invariant_NoDisputeWindowViolations` — the actual property this suite exists to check, previously enforced by nothing but the broken pattern.
- **Both negative controls were run against the FIXED handlers too, not just the original bug** (this project's established double-check discipline): confirmed each suite still passes cleanly on unmodified `src/`, and that reintroducing the same bug it just passed against now correctly fails it. Test-only pass — no `src/` diff; 194 → 196 (two new `invariant_*` functions on existing suites, not new suites).

## New surface added in the participant-limit pass (review focus)
- **`MAX_PARTICIPANTS_LIMIT` enforcement is per-campaign, not global — a founder decision made after discussing the trade-off.** The constant itself (100,000) is now a sanity *ceiling*, not the enforced cap: a global fixed number doesn't fit how differently-sized campaigns work (a small creator airdrop wants a cap of a few hundred; a large brand campaign wants none at all). New `setMaxParticipants(id, cap)` (`CampaignManagement.sol`) lets each host set their own cap (Draft-only, `cap == 0` means unlimited and is the untouched default, `cap > MAX_PARTICIPANTS_LIMIT` reverts `InvalidParticipantLimit`). Matches this codebase's established "configure pieces separately after `createCampaign`" pattern (same shape as `configureERC20Reward`/`addTaskToCampaign`) — deliberately NOT a new `createCampaign` parameter, which would have broken all ~55 existing call sites across the test suite for no correctness benefit.
- **Enforcement point: the same place `totalParticipants` is incremented for a genuinely NEW participant**, in both `completeTask` (self-verify) and `_verifySingleTaskCompletion` (signed-attestation path) — `ParticipantLimitReached` fires before the increment, inside the existing `if (!_hasParticipated[...])` branch. **Deliberately does NOT block a wallet already counted from completing further tasks in the same campaign** — the cap limits how many *unique* participants a campaign can have, not how many tasks an existing participant can complete. Tested explicitly (`test_CompleteTask_CapDoesNotBlockAnAlreadyCountedParticipant`), since this is the kind of off-by-one-in-intent bug that's easy to get wrong (e.g. checking the cap unconditionally instead of only in the new-participant branch would incorrectly lock out a campaign's own already-admitted participants once the cap is hit).
- **Lowering the cap below the current `totalParticipants` does not retroactively remove anyone** — it only blocks further new participants from that point on. No code path exists to reduce `totalParticipants` (nor should one), so this is a natural consequence of the design, not a special case that needed handling.
- **`JOIN_COOLDOWN` was removed rather than implemented — a deliberate scope decision, not an oversight.** It was a 1-minute constant with no enforcement point ever wired to it, and no `join` function exists (participation begins at first task completion). 1 minute is not meaningful sybil resistance regardless of what it gated — a scripted attacker isn't slowed by a 60-second timer — and this project already has a real, specified answer to sybil resistance: Humanity Protocol gating (`docs/HUMANITY_GATING.md`), enforced off-chain at Merkle tree-build time. Wiring up a weak, cosmetic control would have cost EIP-170 headroom (now critically tight, see below) for something that reads as protection without providing any. No existing test referenced it (grep-verified before removal).
- **Contract size**: `Web3Campaigns` 23,678B → 24,045B (~367B for `setMaxParticipants` + its storage mapping/error/event/getter), **~531B headroom remaining — the tightest yet.** This is very close to the EIP-170 ceiling; the next feature touching `Web3Campaigns`'s own inheritance tree directly (not a satellite) will very likely need to be split out or trimmed elsewhere first. See `docs/TEST_AND_BUILD.md`.
- **Tests:** `test/CampaignStorage.t.sol` (+8) — `setMaxParticipants` stores/defaults-to-unlimited/clears-back-to-zero, only-host, Draft-only, rejects above the global ceiling; `completeTask` and `verifyTaskCompletionWithSignature` both reject a new participant once the cap is reached; the already-counted-participant carve-out above.
- **Slither/Aderyn**: Slither 285 → 289 (+4), all accepted classes — naming-convention entries for `setMaxParticipants`'s/`getMaxParticipants`'s `_underscore` params, plus one `timestamp`-comparison false positive on the `CampaignStatus.Draft` enum check (same class as every other status-guard in this codebase, already documented as informational in `docs/TEST_AND_BUILD.md` Known Issues). Aderyn unchanged (High 1 / Low 15).

## New surface added in the permissionless-lifecycle pass (review focus)
- **`endCampaign` is now permissionless (was host-only); `openCampaign` and `closeCampaign` stay host-only — a deliberate, asymmetric split.** Motivation: an "automated campaign platform" should transition campaigns on schedule without depending on the host being online, and — more seriously — a host who opens a campaign and then abandons it (lost wallet, dead project) previously left it stuck in `Open` **forever**: participants could never reach `Ended`, so could never claim, and the escrow could never move. Making `endCampaign` callable by anyone once `block.timestamp >= endTime` closes that lockup and lets the platform keeper run the transition on schedule.
- **Why this is safe: the only precondition a caller cannot influence is the time gate, which the host set.** `endCampaign` still reverts `CampaignNotYetEnded` before `endTime`, `CampaignNotOpen` if not Open, and (newly explicit, since the `onlyHost` modifier that used to provide it is gone) `CampaignNotFound` for a nonexistent campaign. There is **no early-end path for anyone** — permissionless removes *who* can call it, not *when*. A caller can only do what the host's own schedule already authorized: end it at or after the advertised end. Pause coverage is unchanged (the `Web3Campaigns.endCampaign` wrapper's `whenNotPaused` is untouched; only the base's host modifier was removed).
- **`openCampaign` kept host-only, on purpose.** Opening is a judgment call ("configuration is complete, reward is funded, go live"), not a mechanical time transition. Auto-opening the moment a `startTime` passed could take a half-configured or unfunded campaign live and expose participants to a campaign that can never pay out. Draft must stay the host's private staging area with a host-controlled exit. This does **not** reintroduce the lockup `endCampaign` fixes: that lockup occurs strictly *after* a campaign is Open, so requiring the host for the open step is harmless to it.
- **`closeCampaign` kept host-only, on purpose.** Closing freezes the Merkle root (roots are updatable while `Ended`, frozen at `Closed`), so a permissionless close would let a griefer close a campaign the instant it ends and lock the host out of publishing or correcting an allocation root during the dispute window. Closing is also purely a host convenience — it starts the 30-day clock after which the **host** reclaims unclaimed escrow, so no one but the host has a reason to call it. Leaving it host-only creates no lockup: claims work indefinitely in `Ended`, so an abandoned campaign's participants can still claim forever; only the host's leftover-dust reclaim is forgone, which a vanished host would not pursue anyway. A safely time-gated permissionless close (one that respects each root's dispute window rather than allowing an instant freeze) is a possible future refinement — tracked in `docs/NEXT_STEPS.md`, deliberately out of scope here because the close→root-freeze→dispute-window interaction is subtle enough to warrant its own focused change.
- **Contract size went DOWN** (before the `cancelCampaign` follow-up below): 24,045B → 23,988B (~531B → ~588B headroom). Removing the `onlyHost` modifier application and its host-address SLOAD/compare from `endCampaign` saved more than the explicit `CampaignNotFound` check cost — a rare net-negative-size change.
- **Tests** (`test/CampaignStorage.t.sol`, +6): a non-host keeper ends a campaign after `endTime` (the abandoned-host scenario), a non-host still can't end early, `CampaignNotFound`/`CampaignNotOpen` guards, and explicit `StillHostOnly` regression tests for both `openCampaign` and `closeCampaign` so the asymmetry can't be silently eroded. Slither unchanged (289; `endCampaign`'s timestamp comparison was already flagged pre-change), Aderyn unchanged (High 1 / Low 15).

### Follow-up 1: `cancelCampaign` extended to `Ended`, closing a zero-participant refund regression

Caught in PR #20 review (external reviewer + independently reproduced before fixing). Permissionless `endCampaign` had an unintended side effect: `cancelCampaign` only accepted `Draft`/`Open`, so a **zero-participant** campaign whose `endTime` passed and got ended by a keeper (rather than the host, as before this change) permanently lost its `cancelCampaign` immediate-refund path — the host was forced into the 30-day `closeCampaign` → `CLAIM_GRACE_PERIOD` → `withdrawUnclaimedERC20` route for a refund of the exact same amount. Reproduced directly: host funds a campaign, opens it, nobody ever completes a task, `endTime` passes, a non-host caller ends it, host's `cancelCampaign` call reverts `CampaignNotCancellable`.

**Initial fix:** `cancelCampaign`'s status guard was extended to also accept `Ended`, reasoning that `totalParticipants == 0` was "the real safety invariant, not the status." **This reasoning was subsequently found to be incomplete — see Follow-up 2 immediately below, caught in the very next review round before merge.**

### Follow-up 2: settlement-commitment guard added — Follow-up 1 alone reopened a worse rug-pull bug

Caught in a second PR #20 review round (external reviewer + independently reproduced before fixing, same discipline as Follow-up 1). `totalParticipants` tracks `completeTask` engagement only; it is **entirely decoupled from Merkle settlement**. Nothing in `setERC20MerkleRoot`, `claimERC20`, `NFTSettlementModule.setNFTMerkleRoot`, or `claimNFT` reads or writes `totalParticipants`, and a campaign can settle purely from an off-chain allowlist without a single participant ever calling `completeTask`. So Follow-up 1's `Ended`-acceptance, gated only by `totalParticipants == 0`, reopened a **rug-pull path strictly worse than the UX-delay bug it fixed**: a host could publish a Merkle root, let some allocated accounts legitimately claim, then call `cancelCampaign` to instantly reclaim the remaining escrow (no dispute window, no grace period) and permanently lock out every account that hadn't claimed yet — `Web3Campaigns__CampaignNotYetEnded` forever, since `Cancelled` isn't `Ended`/`Closed`. The identical pattern applied to the NFT path via `NFTSettlementModule`, where it is arguably worse: `Cancelled` already makes `withdrawUnclaimedERC721`/`ERC1155` immediately sweepable with **no grace period at all**, so a host could publish an NFT root, let some claim, cancel, and immediately reclaim the specific NFTs allocated to whoever hadn't claimed yet.

Reproduced directly for both paths before fixing (`test_CancelCampaign_RevertsIfSettlementRootPublished_EvenWithZeroParticipants` / `..._RevertsIfNFTSettlementRootPublished_...`, run against the Follow-up-1-only code as a negative control — both attacks succeeded, confirming the gap was real and non-vacuous).

**Fix:** `cancelCampaign` now additionally requires **no settlement has been committed to** on either path:
- ERC20: `_erc20SettlementMode[_campaignId] == ERC20SettlementMode.UNSET`. This single check covers both the Merkle path (`setERC20MerkleRoot` calls `_lockSettlementMode(..., MERKLE)`) and the on-chain tiered path (`OnChainRewardModule` calls back into `setSettlementMode`, itself `_lockSettlementMode`) — mode only ever leaves `UNSET` once a settlement path is committed, and only `Ended`-status actions can commit it, so this check is a structural no-op while Draft/Open and only ever bites in `Ended`, exactly where it's needed.
- NFT: a new `INFTSettlementModule.getNFTMerkleRoot(campaignId)` view (added to the existing trusted-module interface) is queried on the campaign's pinned module (`_campaignNFTModule[_campaignId]`, nonzero only once a deposit has occurred — a root can only ever be set on that same pinned module per `_requireAuthoritative`, so an unpinned campaign trivially passes). A published (nonzero) root blocks cancellation.

Both checks reuse the existing `Web3Campaigns__CampaignNotCancellable` error rather than adding new ones, keeping the fix's bytecode cost down. `test_CancelCampaign_RevertsOnceAParticipantHasEngaged` and Follow-up 1's zero-participant/zero-settlement success case are both unaffected — a genuinely idle campaign (no participants, no settlement committed on either path) still gets its immediate refund exactly as intended.

- **New surface: `cancelCampaign` now makes an external view (staticcall) call to the pinned `NFTSettlementModule` before writing `campaign.status = Cancelled`.** This is a read-only call to a trusted, admin-registered, per-campaign-pinned contract (not attacker-controlled), and `Web3Campaigns.cancelCampaign`'s wrapper already carries `nonReentrant`, so this does not introduce an exploitable reentrancy path — but it does add a new Aderyn `H-1` "state change after external call" instance (7 → 8; still the same single detector category, not a new class of finding), accepted for the reasons above alongside the pre-existing 7 fee-on-transfer-related instances.
- **Tests** (`test/CancelCampaign.t.sol`, +2): `test_CancelCampaign_RevertsIfSettlementRootPublished_EvenWithZeroParticipants` (ERC20 rug-pull reproduction, now reverts), `test_CancelCampaign_RevertsIfNFTSettlementRootPublished_EvenWithZeroParticipants` (NFT equivalent). Both independently verified as non-vacuous via negative control against the Follow-up-1-only code before this fix.
- Contract size after both follow-ups: 23,988B → 24,208B (~588B → ~368B headroom) — still comfortably within the 24,576B limit but the fastest-shrinking headroom yet; see the updated critical-headroom note in `docs/NEXT_STEPS.md`. Aderyn: High 1 (8 instances, +1 from this pass, see above) / Low 15 unchanged.

## ✅ FIXED — Settlement-liveness gap for abandoned Merkle campaigns (`SETTLER_ROLE` fallback pass)

**The gap.** Permissionless `endCampaign` (see the pass above) guarantees a campaign reaches `Ended`, but for Merkle campaigns settlement was still host-only: `setERC20MerkleRoot` (`Web3Campaigns`) and `setNFTMerkleRoot` (`NFTSettlementModule`) could only be called by the campaign host. If a host abandoned a campaign after participants engaged, no root was ever published, nobody could claim, `cancelCampaign` was blocked (`totalParticipants != 0`, and even for a zero-participant campaign the settlement-commitment guard above would eventually block it once a root existed), and escrow stranded forever — a real fund-lockup, structurally identical in shape to the one permissionless `endCampaign` closed for the lifecycle transition itself, just one step later in the flow.

**The fix.** A new `SETTLER_ROLE` (admin-granted/revoked exactly like `SIGNER_ROLE`, granted to the deployer in the constructor chain) and a 14-day `SETTLEMENT_FALLBACK_DELAY` (`CampaignStorage`, internal — see the sizing note below). `setERC20MerkleRoot`, `NFTSettlementModule.setNFTMerkleRoot`, and `closeCampaign` all now accept a `SETTLER_ROLE` caller under a strictly narrower gate than the host:

- **Publish (`setERC20MerkleRoot`/`setNFTMerkleRoot`)**: caller has `SETTLER_ROLE`, campaign is `Ended`, **no root has EVER been published for this campaign on this path**, and `SETTLEMENT_FALLBACK_DELAY` has elapsed since `endTime`. The "never published" condition is the load-bearing one: a settler can only ever fill a vacuum, never override, correct, or race a root the host already set. A settler-published root is otherwise indistinguishable from a host-published one — same `_lockSettlementMode`/mode-commit, same `ROOT_DISPUTE_WINDOW` rearm rule, and the host can still publish/correct a root *after* a settler has acted (a host republish works exactly as it always has, including restarting the dispute window on a genuine value change).
- **Close (`closeCampaign`)**: caller has `SETTLER_ROLE`, campaign is `Ended`, a Merkle root already exists on EITHER path (host- or settler-published — never a settler closing a campaign with nothing ever claimable, which would let a griefer lock in `Closed` prematurely), and the same 14-day delay has elapsed. This exists purely so a truly abandoned campaign can eventually start its 30-day unclaimed-sweep grace clock. **Sweep functions (`withdrawUnclaimedERC20`/`withdrawUnclaimedERC721`/`withdrawUnclaimedERC1155`) are unchanged: host-only, pay only the host.** A settler-triggered close never redirects where the eventual sweep goes — unswept leftover dust of a genuinely abandoned host is that host's own loss, same as before this feature.
- **Composes correctly with the cancelCampaign settlement-commitment guard above**: a settler-published root sets `_erc20MerkleRoot`/the NFT module's root via the exact same code path a host publish would, so `cancelCampaign`'s existing `_erc20SettlementMode != UNSET` / `_nftSettlementPublished` checks catch it identically — no guard changes were needed, verified with explicit tests (`test_CancelCampaign_RevertsAfterSettlerPublishesERC20Root`/`..._NFTRoot`).

**Trust-model note (deliberate, bounded expansion — not a new trust boundary).** The platform's backend already authors 100% of every Merkle tree's contents in the normal flow (see `docs/HUMANITY_GATING.md`'s trust model for sponsored claims/allocation building) — `SETTLER_ROLE` only changes *when* that same backend is allowed to publish a tree it already has the practical ability to construct, and only for a campaign its own host has abandoned for two full weeks. The same `ROOT_DISPUTE_WINDOW` (24h) applies identically to a settler-published root, so the community gets the same review window regardless of who published it. A settler structurally cannot act while a host is exercising authority: the never-published gate means a settler is powerless the instant a host has published anything at all, and the delay gate means a settler is powerless for two weeks even on a genuinely silent campaign. Rotate/revoke via `DEFAULT_ADMIN_ROLE`, identical mechanics to `SIGNER_ROLE` key rotation.

**Sizing — this pass hit the EIP-170 limit and required a real redesign, not just trimming.** `Web3Campaigns` entered this pass at ~368B headroom (the tightest yet). The straightforward implementation — a public `SETTLEMENT_FALLBACK_DELAY`, a dedicated `getCampaignEndTime` cross-contract getter for the NFT module, and a combined settler-eligibility view function — blew the budget by over 180B (measured, not estimated). Landed at **~7B headroom** via three changes, in order of impact:
1. **`SETTLEMENT_FALLBACK_DELAY` made `internal`**, unlike `ROOT_DISPUTE_WINDOW`/`CLAIM_GRACE_PERIOD` (both `public`) — a public constant's auto-generated getter has a real dispatcher-entry cost this budget could no longer absorb. `NFTSettlementModule` keeps its own local `internal constant` copy of the identical 14-day value (the module has ample headroom) instead of reading it cross-contract.
2. **Campaign `endTime` is cached on `NFTSettlementModule` at first deposit** (`recordERC721Deposit`/`recordERC1155Deposit` gained an `endTime` parameter, passed from the two existing `CampaignManagement.sol` call sites that already have it in scope) rather than exposed via a new dedicated getter on `Web3Campaigns` — `endTime` is immutable once a campaign is created, so caching it at deposit time is always correct, and this avoids a new external function entirely.
3. **The module's role check reuses `hasRole`**, already public on `Web3Campaigns` via inherited OZ `AccessControl` (used throughout for every other role's own `onlyRole` checks) — adding it to `IWeb3CampaignsForNFTModule`'s interface costs zero marginal `Web3Campaigns` bytecode, since the function already exists in the deployed contract.
- **This is now the tightest headroom this project has ever shipped at.** Any future inline addition to `Web3Campaigns`/`CampaignManagement`/`ParticipantManagement`/`CampaignViewFunctions`/`CampaignStorage` needs a `forge build --sizes` check before writing a single line, not after — see the updated critical-headroom note in `docs/NEXT_STEPS.md`.
- **New surface: `setERC20MerkleRoot`'s Aderyn `H-1` bucket gained its first instance (8 → 9 total, still one detector category).** The new settler branch calls `hasRole(SETTLER_ROLE, msg.sender)` — an internal call (inherited `AccessControl`, same contract, compiled as a direct JUMP, not even a `STATICCALL`) — before the function's state write; Aderyn's heuristic flags it as an "external call" regardless. Weaker false positive than the pre-existing fee-on-transfer/module-callback instances in this same bucket (those are genuine `STATICCALL`s to `view` functions; this one isn't cross-contract at all), accepted for the same reasons. `closeCampaign` and `NFTSettlementModule.setNFTMerkleRoot` were NOT separately flagged despite an analogous new branch. Low 15 unchanged.
- **Slither: run on both `dev` and `feature/settler-fallback-settlement`, byte-identical detector set and result count — 20 medium-impact findings on each, zero new findings attributable to this feature.** (Slither was unavailable in the environment during initial development of this pass; run independently afterward to close that gap.) All 20 fall into the three classes already documented/accepted elsewhere in this file: the `== 0` sentinel-check pattern (#13, `incorrect-equality` — includes this pass's own `_erc20MerkleRoot[_campaignId] != bytes32(0)`/`== bytes32(0)` vacuum checks, the same accepted pattern as every prior `== 0`/`!= 0` finding in this codebase), the `notifyTaskCompletion`-class reentrancy false-positives (mitigated by `nonReentrant`, same as every other instance in this bucket), and the intentional unused-return destructuring on `(host, status)`-shaped cross-contract reads (the same pattern `NFTSettlementModule._requireHost`/`_requireSweepable` already use, now joined by `setNFTMerkleRoot`'s identical `(address host, CampaignStorage.CampaignStatus status) = ...getCampaignHostAndStatus(...)` call). No new detector category, no new instance count beyond what's already covered by the three accepted classes.
- **Tests**: `test/SettlerFallback.t.sol` (25 unit tests — both ERC20 and NFT paths, every individually-unmet gate condition, host-republish-after-settler, `cancelCampaign` composition, role grant/revoke), `test/invariant/SettlerFallback.invariant.t.sol` + `SettlerFallbackHandler.sol` (an 8th stateful-fuzz suite, ERC20 path only — the NFT path shares identical authorization logic and is exhaustively covered by the unit tests, mirroring this project's established one-suite-per-concern scoping). Ghost-counter pattern used throughout (not loud-revert — see the loud-revert re-verification pass above). Validated non-vacuous two ways: a deterministic sanity test proving a settler CAN legitimately succeed once past the gate, and a negative control (temporarily disabled the delay check in `_requireSettlerRoleAndTiming`, confirmed `invariant_NoSettlerFallbackViolations` fails immediately with a concrete counterexample, then restored) confirming the suite genuinely catches a regression.

## Dropped (2026-07-19) — participant-cap-check gas hoist, reverted for headroom

Attempted the gas-hoist item flagged in the PR #19 review and tracked in `docs/NEXT_STEPS.md`: move the `ParticipantLimitReached` check in `completeTask`/`_verifySingleTaskCompletion` (`ParticipantManagement.sol`) ahead of the state writes/external `_notifyModuleOfCompletion` call/`ONCHAIN_HOLD_*` on-chain verification, so a doomed-to-revert at-capacity attempt fails before spending gas on any of that work rather than after. Implemented correctly — cap == 0 still means unlimited, an already-counted participant is never blocked, `test_CompleteTask_CapDoesNotBlockAnAlreadyCountedParticipant` and every other existing test passed unchanged (244/244).

**Cost 40B of `Web3Campaigns` runtime size (24,569B → 24,609B).** Against the ~7B headroom this contract is down to after the `SETTLER_ROLE` pass above, that would have landed the contract **33B over the EIP-170 limit** — unbuildable, undeployable. Per this pass's hard size gate, reverted rather than pursued via a satellite extraction or other restructuring (that trade was explicitly decided against for this pass). `ParticipantManagement.sol` carries no diff from this attempt. See `docs/NEXT_STEPS.md` for the tracked pick-up note — revisit only once `Web3Campaigns` regains meaningful headroom, or if gas on the rejection path becomes an active pain point worth spending scarce headroom on deliberately.

Related: [[REWARD_SYSTEM]], [[TEST_AND_BUILD]], [[ARCHITECTURE]], [[HUMANITY_GATING]].
